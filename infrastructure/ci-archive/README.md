# Historical CI archive

`Jenkinsfile` is preserved as historical Fusion infrastructure only. It is not an active MansooriKart pipeline and must not be used for deployment. The active CI definition is `.github/workflows/ci.yml`.
